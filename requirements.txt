# --- Core Web Framework ---
# Used to run the web server and handle routes (main.py, etc.)
Flask

# --- Database Management ---
# ORM to handle SQLite database interactions (User creation, storage)
Flask-SQLAlchemy

# --- Authentication ---
# Manages user sessions, login/logout, and cookie handling
Flask-Login

# --- System Monitoring ---
# Used to get disk usage statistics (Total space, used space) for the dashboard
psutil

# How to install?
# Run this command: pip install -r requirements.txt